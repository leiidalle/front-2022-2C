//funciones
//los parametros 'x' se ponen entre las parentesis en la funcion
function Xmas2(x) {
    let res = x +2;
    console.log(res);
    return res;
}

function xMasAlgo(x,algo) {
    return x + algo;
}

//scope donde se declara la variable visible y donde no, es donde 'vive' esa variable